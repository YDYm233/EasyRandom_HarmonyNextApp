# 2026-06-08 工作记录

## 项目扫描 & 文档更新

- 全量扫描了 `app_EasyRandom` 项目（HarmonyOS ArkUI 应用，纯血鸿蒙原生）
- 更新 `README.md`：
  - Badge：API 13+、Version 1.0.19、去掉重复 API 23 badge
  - 技术栈 SDK 版本从 API 12 改为 API 13+
  - 环境要求 SDK 改为 API 13+
  - 📁 项目结构目录树完全重写，精确反映实际磁盘目录（含 form_display、design_privew、entrybackupability、wearable 完整结构等）
  - 版权年份 2024-2025 → 2024-2026
- 更新 `CHANGELOG.md`：
  - v1.0.17 日期补全为 [2026-05-22]，`API 23` 表述规范化
  - v1.0.18 日期格式修正
  - v1.0.19 标记为 `[2026-06-05]~`（当前开发中）

## 手表端实现方案文档

- 初版创建于 `docs/wearable-plan.md`
- 核心设计：横向 Swiper 切换功能 + 纵向 Swiper 浏览数据
- 用户确认交互方式为"横向+纵向组合"，参照 RollWheelCardDisplay 的纵向切换模式

### 方案 v2 修订（本次）

- 文档迁移：`docs/wearable-plan.md` → 根目录 `wearable-plan.md`
- `.gitignore` 新增 `/wearable-plan.md`，本地规划文档不提交到远程仓库
- 删除了空 `docs/` 目录
- **策略变更**：不再直接复用手机端 CardDisplay，改为参考 CardDisplay 逻辑在 wearable 模块内独立建立 `WearXxx` 组件
  - 7 个新建组件：WearRollDices / WearFlipCoin / WearRandomABCD / WearDevineBaGua / WearRollWheel / WearRandomColors / WearBlessingMuyu
  - 1 个数据管理：WearRollDataManager
- **新增 §2 手表端约束**：详细列出硬件约束（屏幕/内存/电池/马达）、ArkUI 约束（组件子集/无键盘/无拖拽）、交互约束（最小触摸区/单屏单任务/动画时长/侧滑冲突）、设计 Token（安全边距/字号/间距等）

### 方案 v3.0 修订（圆/方屏双适配）

- **核心适配策略**："容器层分叉 + 内容层共用" — 圆形屏用 ArcSwiper/Swiper 分叉，内容层同一套 Wear 组件
- 新增 `WearScreenUtil`：屏幕形状检测（display.ScreenShape）+ 尺寸分级（small/standard/large）+ 设计 Token（safePadding/mainImageWidth/resultFontSize 等）
- §2.3 屏幕形状检测 API 封装，§2.5 设计 Token 四象限表
- §3 完整适配架构 + Index.ets 双屏实现代码 + 功能组件内部适配模式示例
- §3.4 圆形屏专用 Arc 组件使用场景表

### 方案 v3.1 重要勘误（ArcSwiper 确认存在）

- ArcSwiper 是 API 18+ 正式独立组件（`@kit.ArkUI`），非"标准 Swiper + Circle 裁切"
- 文档全面重写：§2.2 完整 ArcSwiper API 参考、§3.2 Index.ets 双屏实现、§4.1 交互模型图等
- 新增 ArcSwiperAttribute 手动导入（API ≤21）问题记录

### 方案 v3.2 修订（四层适配模型 + 设计原则）

- §3.1 重构为四层模型：检测层→容器层→内容层→工具层
- 新增 §3.5 关键设计原则（三条铁律）

### 方案 v3.3 修订（架构评审 7 项修复）

**P1 修复（5 项）：**
1. §4.2 表格硬编码值 → Token 引用（`mainImageWidth`(55%) 等），新增禁止硬编码注释
2. §8 子节编号 7.1/7.2/7.3 → 8.1/8.2/8.3
3. display 模块 import 添加 ⚠️ 待确认注释（可能从 `@ohos.display` 导入），新增 §6 问题 #7
4. 全部 7 个 Token getter 显式添加 large 分支，同步更新 §2.3 和 §2.5 Token 表
5. §3.3 新增 ArcButton 完整示例代码（WearBlessingMuyu + ArcButtonPosition）

**P2 修复（2 项）：**
6. screenWidth/screenHeight 抽取 `_initDimensions()` 共享方法
7. scaledSize() 添加详细设计注释（基准偏差说明 + 适用场景限定）

### 手表端代码实现完成（Step 1-5）

按照 wearable-plan.md v3.3 执行了完整的代码实现：

**Step 1 — 启用模块**：
- `build-profile.json5`：取消 wearable 模块注释
- `product/wearable/src/main/module.json5`：取消全部注释，添加 `ohos.permission.VIBRATE` 权限
- `string.json`：添加 `vibrate_reason` 字符串

**Step 2 — WearScreenUtil.ets**：
- 新建 `product/wearable/src/main/ets/utils/WearScreenUtil.ets`
- 确认 `display` 模块正确 import 路径为 `@ohos.display`（手机端 DisplayMarquee.ets 已验证）
- 实现：形状检测（isRoundScreen/isSquareScreen）、尺寸分级（small/standard/large）、7 个 Token getter（全部含 large 分支）、scaledSize() 缩放工具、_initDimensions() 共享初始化

**Step 3 — 7 个 Wear 组件 + WearRollDataManager**：
- `WearRollDataManager.ets`：精简版转盘数据管理（无 Preferences 持久化，仅默认数据 + 工具函数）
- `WearRollDices.ets`：骰子图片随机切换
- `WearFlipCoin.ets`：硬币翻转动画（rotate x轴 + Random）
- `WearRandomABCD.ets`：四格直铺 + 旋转指针，setTimeout 延迟高亮
- `WearDevineBaGua.ets`：八卦图随机旋转
- `WearRollWheel.ets`：Canvas 扇形绘制 + 内层 Swiper 切换多转盘
- `WearRandomColors.ets`：全屏着色 + 色值显示
- `WearBlessingMuyu.ets`：木鱼敲击 + 气泡弹跳动画
- 全部组件使用 `WearScreenUtil` Token（零硬编码），无 `@ohos/hypium` 依赖

**Step 4 — Index.ets + 资源**：
- 重写 `Index.ets`：`if(isRound)` → ArcSwiper（ArcDotIndicator + CrownSensitivity） vs Swiper（DotIndicator + loop）
- 清理旧的 `Contexts/ABCDCard.ets` 原型
- 复制 10 个图片资源：dice1-6.png、coin1-2.png、BaGua.png、MuYu.png
- 颜色资源确认在 AppScope 全局共享，无需复制

**Step 5 — 验证**：
- 去除 `WearRollWheel.ets` 中未使用的 `currentIndex` 变量
- 确认所有颜色 Token（Brand、Comp_Bg4/Bg2、FontIcon_Fore1/Fore2/Back1）在 AppScope 中已定义
- 确认 needle.svg、pointer_m.svg 在 AppScope 中全局可用
- 文件结构：4 个已有 + 10 个新建 = 14 个 .ets 文件

**待验证项（需 DevEco Studio）**：
- ArcSwiperAttribute 在 API ≤ 21 时需手动 import
- 编译构建是否通过
- 圆/方屏模拟器布局效果

### SDK Import 路径修正（v3.4 实测验证）

编译过程中发现所有 `@kit.ArkUI` import 路径在 SDK 中实际不匹配，通过直接读取 SDK 中的 .d.ts 文件逐一确认：

| 组件/模块 | 错误写法（文档 v3.3） | 正确写法（v3.4 实测） |
|----------|---------------------|---------------------|
| ArcSwiperController, ArcDotIndicator, ArcDirection | `@kit.ArkUI` | `@ohos.arkui.ArcSwiper` |
| ArcButtonPosition | `@kit.ArkUI` | `@ohos.arkui.advanced.ArcButton` |
| display | `@kit.ArkUI` (存疑) | `@ohos.display` (手机端已确认) |
| CrownSensitivity | 需要 import | 全局枚举，API 18+ 自动可用 |
| ArcSwiper 组件名 | 需要 import | 导入 `@ohos.arkui.ArcSwiper` 后自动注册 |

**关键发现**：Arc 组件名（如 ArcSwiper、ArcButton）不需要显式 import，只需要 import 模块中的类型（controller/attribute/enum），组件名会自动注册到全局作用域。

**构建配置修正历程**：
1. `build-profile.json5` 取消 wearable 模块注释 → 缺逗号 JSON5 语法错误
2. `applyToProducts: []` → minItems 不允许空数组，移除整个 targets 块
3. `compatibleSdkVersion: "5.0.2(18)"` → 平台号与 API 不匹配，改为 `"5.1.0(18)"`（SDK 中 API 18 = 5.1.0）
4. wearable 模块级 `build-profile.json5` 不能设 `compatibleSdkVersion`（不在 schema 允许字段中），只能通过根 product 级别设置

**wearable-plan.md 同步更新至 v3.4**：
- §2.2 Arc 组件表 import 路径全修正
- §2.3 display import 路径确认
- §3.2 Index.ets 示例代码 import 修正
- §3.3 ArcButton 示例 import 修正
- §6 问题 #7 标记已确认

### ArcSwiper API 修正（v3.5 SDK 实测）

编译发现 Index.ets 中 ArcSwiper/ArcDotIndicator 的实际 API 与之前假设不符。通过读取 SDK 真实 .d.ts 声明文件确认：

| 问题 | 之前（v3.4） | 修正后（v3.5） |
|------|------------|-------------|
| Import 路径 | `@ohos.arkui.ArcSwiper` | `@kit.ArkUI`（SDK kit 入口 re-export，更规范） |
| ArcDotIndicator 颜色 | `.color()` / `.selectedColor()` | `.itemColor()` / `.selectedItemColor()` |
| ArcDotIndicator 阴影 | `.itemShadow(true)` | **不存在此方法**（SDK 中无 itemShadow） |
| ArcSwiper 构造函数 | `{ controller, indicator }` (对象参数) | **仅 `controller`** (可选参数，indicator 通过 `.indicator()` 链式设置) |
| ArcSwiperAttribute | 需要手动 import | `@kit.ArkUI` 已 re-export，自动可用 |

**根本原因**：SDK 声明文件中 `ArcSwiper` 的构造函数签名是 `(controller?: ArcSwiperController): ArcSwiperAttribute`，只接受 controller 参数。indicator 等属性通过 `ArcSwiperAttribute` 的 `.indicator()` 方法设置。

wearable-plan.md → v3.5（同步更新此修正）
